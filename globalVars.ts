(global as any).registeredArticles = [] as Array<string>;

// Type declaration for TypeScript
declare global {
  var registeredArticles: Array<string>;
}

// Export to make it available for imports if needed
export {};
